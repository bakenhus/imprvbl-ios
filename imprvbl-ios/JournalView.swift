import SwiftUI

struct JournalView: View {
    var body: some View {
        ScrollView(showsIndicators: true, content: {
            Text("Placeholder")
        })
    }
}

#Preview {
    JournalView()
}
